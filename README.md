# DATA-SCIENCE-PROJECT
Transportation Delay Classification &amp; Trip Duration Prediction using machine learning. The project includes data preprocessing, EDA, feature engineering, model training, hyperparameter tuning with GridSearchCV, evaluation, model persistence, and prediction on unseen data using Python, Pandas, NumPy, Matplotlib, and Scikit-learn.
